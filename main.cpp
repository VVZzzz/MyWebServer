#include <string>

int main(int argc , char **argv) {
    //默认开启线程数和端口
    int threadNum = 4;
    int port = 80;
    //解析命令行
    int opt;
    const char*optstr = "ptlog";
    while (opt = )
    {
    }
    

}